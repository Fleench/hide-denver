import 'package:flutter_test/flutter_test.dart';
import 'package:hide_denver/main.dart';

void main() {
  test('app shell can be constructed', () {
    expect(const HideDenverApp(), isA<HideDenverApp>());
  });
}
