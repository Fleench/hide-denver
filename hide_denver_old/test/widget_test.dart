import 'package:flutter_test/flutter_test.dart';

import 'package:hide_denver/main.dart';

void main() {
  test('app widget is constructible', () {
    expect(const MissionCompanionApp(), isA<MissionCompanionApp>());
  });
}
